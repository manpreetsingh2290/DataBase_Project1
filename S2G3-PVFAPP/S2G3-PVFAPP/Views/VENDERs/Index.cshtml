@model IEnumerable<S2G3_PVFAPP.Models.VENDER>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Vender_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vender_Address)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Vender_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vender_Address)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Vender_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Vender_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Vender_ID })
        </td>
    </tr>
}

</table>
