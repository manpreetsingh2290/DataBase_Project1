@model IEnumerable<S2G3_PVFAPP.Models.SALESPERSON>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Salesperson_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salesperson_Telephone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salesperson_Fax)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TERRITORY.Territory_ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Salesperson_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Salesperson_Telephone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Salesperson_Fax)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TERRITORY.Territory_ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Salesperson_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Salesperson_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Salesperson_ID })
        </td>
    </tr>
}

</table>
